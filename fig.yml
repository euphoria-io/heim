frontend:
  command: gulp build
  build: client
  volumes:
    - client:/srv/heim/client/src
    - backend/static:/srv/heim/client/src/build

backend:
  build: backend
  volumes:
    - backend/static:/go/src/heim/backend/static
  ports:
    - "8080:80"
